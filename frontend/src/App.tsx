import "./quick.css";
import { FormEvent, useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Category, login, request, User } from "./api";
import { InvoiceEntry } from "./InvoiceEntry";
import { DashboardView, SettingsView } from "./OperationsViews";
import { CustomerPerformanceView, InvoiceHistoryView, LedgerView, PartiesView, PaymentView, ReportsView, ReturnView, SupplierPerformanceView } from "./RecordsViews";
import { FinanceWorkspaceView } from "./FinanceWorkspaceView";
import { TokenView } from "./TokenView";
import { InventoryWorkspaceView } from "./InventoryWorkspaceView";
type Module = "sales" | "purchases" | "inventory" | "customers" | "suppliers" | "finance" | "reports" | "settings" | "tokens";
type Screen = "dashboard" | `${Module}-menu` | "sales" | "purchases" | "inventory" | "customers" | "suppliers" | "finance" | "reports" | "settings" | "tokens" | "sales-history" | "purchase-history" | "customer-payments" | "supplier-payments" | "customer-ledger" | "supplier-ledger" | "customer-performance" | "supplier-performance" | "sales-returns" | "purchase-returns";
const moduleMeta: Record<Module, { title: string; description: string; icon: string }> = { sales: { title: "Sales", description: "Sell paint, receive payments and manage sales", icon: "▣" }, purchases: { title: "Purchases", description: "Record purchases, supplier payments and returns", icon: "▤" }, inventory: { title: "Inventory", description: "Products, stock levels and stock adjustments", icon: "▦" }, customers: { title: "Customers", description: "Customers, balances, performance and payments", icon: "♙" }, suppliers: { title: "Suppliers", description: "Suppliers, balances, performance and payments", icon: "♜" }, finance: { title: "Finance", description: "Expenses, cash, bank transactions and party payments", icon: "₨" }, reports: { title: "Reports", description: "Business performance and accounting reports", icon: "▥" }, settings: { title: "Settings", description: "Business details, users and configuration", icon: "⚙" }, tokens: { title: "Tokens", description: "Coupon inventory, painter claims and reimbursements", icon: "🎟" } };
const allowedModules = (role: string): Module[] => role === "admin" ? ["sales", "purchases", "inventory", "customers", "suppliers", "finance", "reports", "settings", "tokens"] : role === "accountant" ? ["sales", "purchases", "customers", "suppliers", "finance", "reports", "tokens"] : role === "inventory_manager" ? ["purchases", "inventory", "suppliers", "reports", "tokens"] : ["sales", "customers", "inventory"];
const actions: Record<Module, { screen: Exclude<Screen, "dashboard" | `${Module}-menu`>; label: string; description: string }[]> = { sales: [{ screen: "sales", label: "New Sale", description: "Create and post a customer invoice" }, { screen: "sales-history", label: "Sales History", description: "View, search and print invoices" }, { screen: "sales-returns", label: "Sales Returns", description: "Process returned customer goods" }, { screen: "customer-payments", label: "Receive Payment", description: "Record money received from customers" }], purchases: [{ screen: "purchases", label: "New Purchase", description: "Record a supplier purchase" }, { screen: "purchase-history", label: "Purchase History", description: "View and manage purchase invoices" }, { screen: "purchase-returns", label: "Purchase Returns", description: "Return goods to suppliers" }, { screen: "supplier-payments", label: "Pay Supplier", description: "Record payments to suppliers" }], inventory: [{ screen: "inventory", label: "Manage Inventory", description: "Products, fast stock search and movement history" }], customers: [{ screen: "customers", label: "Customer List", description: "Add and manage customers" }, { screen: "customer-performance", label: "Customer Performance", description: "Buying volume, profit margin, returns and trends" }, { screen: "customer-payments", label: "Receive Payment", description: "Allocate customer payments to invoices" }, { screen: "customer-ledger", label: "Customer Ledger", description: "View customer balances and transactions" }], suppliers: [{ screen: "suppliers", label: "Supplier List", description: "Add and manage suppliers" }, { screen: "supplier-performance", label: "Supplier Performance", description: "Purchase volume, returns, payments and trends" }, { screen: "supplier-payments", label: "Pay Supplier", description: "Allocate supplier payments to invoices" }, { screen: "supplier-ledger", label: "Supplier Ledger", description: "View supplier balances and transactions" }], finance: [{ screen: "finance", label: "Finance & Payments", description: "Expenses, cash/bank, customer receipts and supplier payments" }], reports: [{ screen: "reports", label: "Business Reports", description: "Accounting reports and financial information" }], settings: [{ screen: "settings", label: "Business Settings", description: "Business information and user administration" }], tokens: [{ screen: "tokens", label: "Token Inventory", description: "Track coupon inventory from purchases, sales and returns" }] };
function Login({ onLogin }: { onLogin: () => void }) { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const mutation = useMutation({ mutationFn: () => login(email, password), onSuccess: ({ access_token }) => { localStorage.setItem("access_token", access_token); onLogin(); } }); const submit = (event: FormEvent) => { event.preventDefault(); mutation.mutate(); }; return <main className="login"><form onSubmit={submit}><div className="brand-mark">PS</div><h1>Paint Shop ERP</h1><p>Sign in to manage your business.</p><label>Email<input type="email" required value={email} onChange={e => setEmail(e.target.value)} /></label><label>Password<input type="password" required value={password} onChange={e => setPassword(e.target.value)} /></label>{mutation.error && <p className="error">{mutation.error.message}</p>}<button disabled={mutation.isPending}>{mutation.isPending ? "Signing in…" : "Sign in"}</button></form></main>; }
function ModuleMenu({ module, onNavigate, onBack }: { module: Module; onNavigate: (screen: Screen) => void; onBack: () => void }) { const meta = moduleMeta[module]; return <section className="module-page"><button type="button" className="back-button" onClick={onBack}>← Dashboard</button><div className="page-heading"><div className="module-icon large">{meta.icon}</div><div><p className="eyebrow">{meta.title.toUpperCase()}</p><h1>{meta.title}</h1><p>{meta.description}</p></div></div><div className="action-grid">{actions[module].map(action => <button type="button" className="action-card" key={action.screen} onClick={() => onNavigate(action.screen)}><span className="action-arrow">→</span><strong>{action.label}</strong><small>{action.description}</small></button>)}</div></section>; }
function Dashboard({ user }: { user: User }) { const queryClient = useQueryClient(); const [name, setName] = useState(""); const [screen, setScreen] = useState<Screen>("dashboard"); const categories = useQuery({ queryKey: ["categories"], queryFn: () => request<Category[]>("/categories") }); const addCategory = useMutation({ mutationFn: () => request<Category>("/categories", { method: "POST", body: JSON.stringify({ name }) }), onSuccess: () => { setName(""); queryClient.invalidateQueries({ queryKey: ["categories"] }); } }); const modules = allowedModules(user.role); const dashboard = <><DashboardView /><section className="quick-section"><div className="section-heading"><div><p className="eyebrow">QUICK ACCESS</p><h2>What would you like to do?</h2></div><span className="role-badge">{user.role.replace("_", " ")}</span></div><div className="module-grid">{modules.map(module => { const meta = moduleMeta[module]; return <button type="button" className="module-card" key={module} onClick={() => setScreen(`${module}-menu`)}><span className="module-icon">{meta.icon}</span><span className="module-copy"><strong>{meta.title}</strong><small>{meta.description}</small></span><span className="module-arrow">→</span></button>; })}</div></section><section className="panel category-panel"><h2>Product categories</h2><p>Create the paint categories you sell.</p><form className="inline" onSubmit={e => { e.preventDefault(); if (name.trim()) addCategory.mutate(); }}><input aria-label="Category name" placeholder="e.g. Interior Emulsion" value={name} onChange={e => setName(e.target.value)} /><button type="submit" disabled={addCategory.isPending}>Add category</button></form>{addCategory.error && <p className="error">{addCategory.error.message}</p>}{categories.isPending ? <p>Loading categories…</p> : <div className="category-list">{categories.data?.map(category => <span key={category.id}>{category.name}</span>)}</div>}</section></>;
  const activeModule = (Object.keys(moduleMeta) as Module[]).find(item => screen === `${item}-menu` || actions[item].some(action => action.screen === screen)); const isModuleMenu = screen.endsWith("-menu"); const content = isModuleMenu ? <ModuleMenu module={screen.replace("-menu", "") as Module} onNavigate={setScreen} onBack={() => setScreen("dashboard")} /> : screen === "dashboard" ? dashboard : screen === "sales" ? <InvoiceEntry kind="sale" /> : screen === "purchases" ? <InvoiceEntry kind="purchase" /> : screen === "inventory" ? <InventoryWorkspaceView /> : screen === "settings" ? <SettingsView /> : screen === "finance" ? <FinanceWorkspaceView /> : screen === "tokens" ? <TokenView /> : screen === "customers" ? <PartiesView kind="customer" /> : screen === "suppliers" ? <PartiesView kind="supplier" /> : screen === "sales-history" ? <InvoiceHistoryView kind="sale" /> : screen === "purchase-history" ? <InvoiceHistoryView kind="purchase" /> : screen === "customer-payments" ? <PaymentView kind="customer" /> : screen === "supplier-payments" ? <PaymentView kind="supplier" /> : screen === "customer-ledger" ? <LedgerView kind="customer" /> : screen === "supplier-ledger" ? <LedgerView kind="supplier" /> : screen === "customer-performance" ? <CustomerPerformanceView /> : screen === "supplier-performance" ? <SupplierPerformanceView /> : screen === "reports" ? <ReportsView /> : screen === "sales-returns" ? <ReturnView kind="sale" /> : <ReturnView kind="purchase" />;
  return <div className="app-shell"><header className="topbar"><button type="button" className="brand-button" onClick={() => setScreen("dashboard")}><span className="brand-mark small">PS</span><span>Paint Shop <b>ERP</b></span></button><div className="topbar-user"><div><strong>{user.full_name}</strong><small>{user.role.replace("_", " ")}</small></div><button type="button" className="secondary" onClick={() => { localStorage.removeItem("access_token"); location.reload(); }}>Sign out</button></div></header><main className="content">{!isModuleMenu && screen !== "dashboard" && <button type="button" className="back-button" onClick={() => setScreen(activeModule ? `${activeModule}-menu` : "dashboard")}>← Back to {activeModule ? moduleMeta[activeModule].title : "Dashboard"}</button>}{content}</main></div>; }
export function App() { const [authenticated, setAuthenticated] = useState(Boolean(localStorage.getItem("access_token"))); const me = useQuery({ queryKey: ["me"], queryFn: () => request<User>("/auth/me"), enabled: authenticated, retry: false }); if (!authenticated) return <Login onLogin={() => setAuthenticated(true)} />; if (me.isPending) return <main className="login"><p>Loading your workspace…</p></main>; if (me.error || !me.data) { localStorage.removeItem("access_token"); return <Login onLogin={() => setAuthenticated(true)} />; } return <Dashboard user={me.data} />; }
